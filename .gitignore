##############################################
# itemsety-qwen-finetuning .gitignore
# Last updated: 2026-04-07
##############################################

# ── Secrets & environment (CRITICAL — never commit!) ──
*.env
.env
*.key
*.pem

# ── Virtual environments ──
.venv/
venv/
ENV/
env/

# ── Python cache & compiled artifacts ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.dll
*.egg-info/
.eggs/
build/
dist/
*$py.class
*.whl
*.spec

# ── Jupyter ──
.ipynb_checkpoints/

# ── Editors / IDE ──
.vscode/
.idea/
*.swp
*.swo
*.tmp
*~

# ── Obsidian vault config (track content, ignore app settings) ──
obsidian-brain/.obsidian/

# ── Database & logs ──
*.log
*.sqlite
*.db-journal
*.db-wal
runs.db
logs/

# ── Pipeline artifacts (regenerable from pipeline.py) ──
artifacts/
visuals/

# ── Training infrastructure (lives on GPU server) ──
checkpoints/
runs/
wandb/

# ── Large CSVs (500 datasets tracked via data/datasets_v2/) ──
*.csv
!README.md
!requirements.txt

# ── Temporary / scratch files ──
temp_*.csv
scratch_*.py
ds_TEST.csv
master_seed.txt

# ── Caches ──
.cache/
.mypy_cache/
.pytest_cache/

# ── Coverage ──
.coverage
htmlcov/

# ── OS files ──
.DS_Store
Thumbs.db
Desktop.ini

# ── Archives (zip kept for TLJH upload reproducibility) ──
datasets_v2.zip
