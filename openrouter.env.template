# OpenRouter API Configuration
# Used by: src/utils/openrouter_client.py  →  src/evaluation/council_advisor.py
#
# Copy this file to openrouter.env and fill in your key:
#   cp openrouter.env.template openrouter.env
#
# Get your key at https://openrouter.ai/
# Make sure to add credits or enable auto top-up before running council analysis.

OPENROUTER_API_KEY=sk-or-v1-your-key-here

# ---------------------------------------------------------------------------
# Optional: override default council models (comma-separated OpenRouter ids)
# These defaults are used by CouncilAdvisor if not overridden via CLI / code.
#
# COUNCIL_MODELS=anthropic/claude-sonnet-4.6,google/gemini-3-flash-preview,deepseek/deepseek-v3.2,x-ai/grok-4.1-fast
# CHAIRMAN_MODEL=anthropic/claude-opus-4.6
# ---------------------------------------------------------------------------
